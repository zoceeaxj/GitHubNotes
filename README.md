1. git init : Initialize the folder, you initialize it only once, .git will be in that folder
	a. if you do not want anything to include any thing from your eclipse project you can create a gitignore file in eclipse and add those files.
	b. https://www.toptal.com/developers/gitignore
2. git add . (. means include everything)
3. git commit -m "Write your message here" (-m means message)
4. Github/new repository
5. Bash: git remote -v 
6. git remote add origin URL (origin is remote name)
7. git push origin MASTER (master is branch)
